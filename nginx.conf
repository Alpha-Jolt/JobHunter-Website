server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Prerendered routes (about/, features/, etc.) + SPA fallback
    location / {
        try_files $uri $uri/ $uri/index.html /index.html;
    }
}
